#include<stdio.h>
int main(){
    int a, b;
    scanf("%d", &a);
    int ranke[200000];
    for (int i = o;i<a; i++) scanf("%d",&ranked[i]);

    scanf("%d", &b);
    int player[200000];
    for (int i = 0;i<b; i++);
    int result[200000];

    for(int i = 0; i<b; i++)
    printf("%d\n",result[i]);

    return 0;
}