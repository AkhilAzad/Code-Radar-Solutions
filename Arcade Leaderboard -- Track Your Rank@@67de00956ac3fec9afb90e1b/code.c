#include<stdio.h>
int main(){
    int a, b;
    scanf("%d", &a);
    int rank[200000];
    for (int i = 0;i<a; i++) scanf("%d", &rank[i]);

    scanf("%d", &b);
    int player[200000];
    for (int i = 0;i<b; i++);
    int result[200000];

    for(int i = 0; i<b; i++)
    printf("%d\n",result[i]);

    return 0;
}